# TODO for the web based version

Document each change once done in CHANGES.md. Then remove from TODO.md

* Modulus should be allowed in JSON it should have units of GPa and MPa (all command line parameters should have JSON equivalents, CLI overrides JSON)
* Check again for convergence speed with the new thrust-maximizing goal.
